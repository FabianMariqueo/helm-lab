apiVersion: v1
kind: Service
metadata:
  name: {{ include "example-functions.fullname" . }}
  labels:
    {{- include "example-functions.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "example-functions.selectorLabels" . | nindent 4 }}
