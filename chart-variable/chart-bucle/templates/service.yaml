apiVersion: v1
kind: Service
metadata:
  name: {{ include "chart-bucle.fullname" . }}
  labels:
    {{- include "chart-bucle.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chart-bucle.selectorLabels" . | nindent 4 }}
