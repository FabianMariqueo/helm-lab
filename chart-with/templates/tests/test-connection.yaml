apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chart-with.fullname" . }}-test-connection"
  labels:
    {{- include "chart-with.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "chart-with.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
