apiVersion: v1
kind: Service
metadata:
  name: {{ include "chart-with.fullname" . }}
  labels:
    {{- include "chart-with.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chart-with.selectorLabels" . | nindent 4 }}
